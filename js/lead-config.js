/**
 * TrackingmeCA lead-capture endpoint.
 *
 * CRM: a Google Sheet, written to by the Apps Script Web App in
 * tools/apps-script/Code.gs. Each submission is also emailed to
 * info@trackingme.ca — see that script's NOTIFY_EMAIL constant.
 *
 * Replace the placeholder below with the deployed Web App URL
 * (Deploy > Manage deployments > Web app, in the Apps Script editor).
 * Until it's replaced, submissions are logged to the console instead of
 * sent anywhere — nothing is lost, but nothing is delivered either.
 */
window.LEAD_ENDPOINT = "REPLACE_WITH_APPS_SCRIPT_WEB_APP_URL";
